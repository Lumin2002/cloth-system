# 服装订单系统需求分析与实施方案

## 一、项目现状

本项目是 **Django 5.2 + SQLite/MySQL** 的服装面料订单管理系统，不是 Spring Boot。

核心业务集中在 `order` 应用：

- `order/models.py`：`ClothOrder` 订单主表、`Shipment` 出货表、`FabricQuotation` 面料报价表等。
- `order/views_orders.py`：管理端订单列表、详情、进度、对账单。
- `order/views_supplier.py`：供应商报价、出货、供应商端订单。
- `order/forms.py`：订单表单、供应商价格表单、出货表单。
- `order/statement.py`：当前对账单生成逻辑。
- `order/order_excel.py`：订单 Excel 导入导出。

现有价格单位是自由文本，前端会按“数量单位是米/码”自动填入“元/米/元/码”。因此现在不能自由选择美金单位，也不支持价格历史参考。

现有对账单由代码 `statement.py` 写死一种表格布局，无法做到“客户给什么模板就用什么模板”。

现有出货后进度更新逻辑写死为 `set_progress_stage("剪版寄出")`，该阶段只存在于样板单进度中，因此大货订单出货后不会进入“发货”阶段。

---

## 二、需求逐项分析

### 1. 价格参考历史，分大货和样板

**目标**

新增/编辑订单时，选择“大货订单 / 大货-印花 / 样品订单”后，自动提示同类型订单最近 5 次的历史价格，供录单人员参考。

**实现方式**

1. 后端新增一个只读接口：
   - URL：`/orders/price-history/`
   - 接收参数：`order_type`、`cloth_type`、`customer`、`color`、`exclude`
   - 规则：
     - `bulk` 与 `bulk_print` 归为一组“大货”。
     - `sample` 归为“样板”。
     - 有 `cloth_type` 时优先按布种过滤；有 `customer` 或 `color` 时进一步收窄。
     - 排除当前正在编辑的订单。
   - 返回最近 5 条记录：下单日期、序号、客户、布种、颜色、单价、价格单位、数量、数量单位。

2. 前端在新增订单、编辑订单、订单详情编辑态中，监听：
   - `订单类型` 变化；
   - `布种` 失去焦点；
   - `客户` 失去焦点。

   然后异步调用接口，并在价格输入框下方展示“历史价格参考”卡片。

**实现效果**

选择大货或样板单后，页面会列出同类型最近 5 次价格。录入人员不用去翻历史订单或 Excel，可快速判断本次报价是否合理。

**难度**

中低。主要是新增一个查询接口和前端展示，不需要改数据库结构。

---

### 2. 送货单、对账单使用客户指定模板

**目标**

送货单和对账单必须与客户提供的一模一样，并支持后续增加模板。

**实现方式**

推荐做“模板文件 + 列映射填充”的生成机制，而不是继续在代码里写死布局：

1. 管理员上传客户原始 `.xlsx` 模板文件。
2. 系统记录模板类型：
   - `对账单`
   - `送货单`
3. 生成单据时：
   - 用 `openpyxl` 读取模板文件；
   - 保留原始样式、合并单元格、列宽、图片、公式；
   - 根据模板表头自动找到对应列；
   - 将订单/出货数据写入对应列；
   - 合计行保留模板原有公式或自动补充。

**实现效果**

客户提供什么模板，系统就输出什么模板。新增模板不用改代码，只需上传并标记默认模板。

**难度**

中高。需要新增模板存储模型、上传/选择页面、模板解析和动态列映射，并验证多种模板格式。建议先支持“一个默认对账单模板”，再扩展送货单模板。

---

### 3. 大货订单出货后更新状态为“发货”

**问题原因**

`order_shipment_create` 和 `order_refresh_calculations` 中写死：

```python
order.set_progress_stage("剪版寄出")
```

大货进度阶段中没有“剪版寄出”，所以调用失败，进度不会更新。

**实现方式**

将“出货后推进阶段”封装为订单模型方法：

```python
def advance_to_shipped_stage(self):
    # 样板单推进到“剪版寄出”
    # 大货订单推进到“发货”
    # 其他类型按阶段表匹配
```

然后在所有登记出货、刷新金额、供应商保存出货记录的地方统一调用。

**关于“是否需要重新更换进度流转内容”**

建议先不要大改。当前问题不是流程设计错，而是代码写死了一个只属于样板单的阶段名。

推荐的默认流程保持：

- 大货：`客户下单 → 通知供应商 → 大货样 → 批色 → 查布 → 发货 → 待收款 → 已完成`
- 样品：`客户下单 → 通知供应商 → 剪版寄出 → 待收款 → 已完成`

先把“大货出货后自动推进到‘发货’”修复，再根据实际业务看是否要增加“质检、物流、收货确认”等阶段。

**难度**

低。改后端逻辑即可，不改数据库结构。

---

### 4. 付款申请单、报销单一键生成

**是否适合用 Spring Boot？**

不适合为了这两个单据单独改造成 Spring Boot。

原因：

1. 当前系统是 Django，订单、供应商、财务数据都已在 Django/SQLite/MySQL 中。
2. 付款申请单和报销单本质是“模板 + 表单数据 + Excel/PDF 生成”，Django + `openpyxl` 或 `reportlab` 完全可以实现。
3. 如果引入 Spring Boot，需要重新搭服务、连接数据库、做用户体系，维护成本和系统复杂度远高于收益。

**建议**

继续保持 Django 实现。可以复用第 2 点的“模板管理机制”，把付款申请单、报销单作为新的模板类型接入。

本阶段可暂时不做，但架构上预留 `template_type` 类型即可。

**难度**

若用 Django 实现：中低。若硬改成 Spring Boot：高，且需要额外部署和维护。

---

### 5. 价格单位改为四个固定选项

**目标**

价格单位固定为：

- 元/米
- 元/码
- 美金/米
- 美金/码

**实现方式**

1. 在 `order/constants.py` 中定义统一常量 `PRICE_UNIT_CHOICES`。
2. 在订单表单中把 `price_unit` 从只读文本框改为下拉框。
3. 移除“根据数量单位自动改成元/米或元/码”的旧逻辑。
4. Excel 导入时兼容原有“元/米”“元/码”以及新的“美金/米”“美金/码”。

**实现效果**

录入人员可以直接选择四种价格单位，不再受数量单位限制。历史数据仍然兼容。

**难度**

低。主要是表单控件和前端联动调整。

---

## 三、建议实施顺序

| 顺序 | 需求 | 风险 | 是否立即实施 |
|---|---|---|---|
| 1 | 价格单位四选一 | 低 | 是 |
| 2 | 价格历史参考 | 中低 | 是 |
| 3 | 大货出货后进度推进 | 低 | 是 |
| 4 | 对账单/送货单模板化 | 中高 | 先搭模板生成基础，再逐步扩展 |
| 5 | 付款申请单/报销单 | 中低 | 暂缓，沿用 Django 方案 |

---

## 四、当前工作分支

代码修改在独立分支进行：

```text
codex/price-unit-templates-progress
```

后续每完成一项都会跑 Django 检查，确保不破坏现有导入、列表、详情、供应商端功能。

---

## 五、实施进度

### 已完成

1. **价格单位四选一**
   - `order/constants.py` 新增 `PRICE_UNIT_CHOICES`。
   - `order/forms.py` 将 `price_unit` 改为下拉框。
   - 移除新增/编辑/详情页中“按数量单位自动改写价格单位”的旧逻辑。

2. **价格历史参考**
   - 新增接口 `GET /orders/price-history/`。
   - 新增模板片段 `order/templates/order/_price_history.html`。
   - 新增订单、编辑订单、订单详情编辑态均展示同类型最近 5 次价格，点击可回填价格和价格单位。

3. **大货出货后推进到“发货”**
   - `ClothOrder` 新增 `advance_to_shipped_stage()`。
   - 管理端、供应商端登记出货后按订单类型推进：
     - 样板单：`剪版寄出`
     - 大货/印花/其他：`发货`
   - 已更新对应测试。
   - 已对现有数据执行一次性补齐：共更新 602 条“已有出货但进度未推进”的有效订单。
   - 数据操作前已备份：
     - `db.sqlite3.bak_price_progress_20260908`

### 待客户提供资料后继续

4. **对账单/送货单模板**
   - 需要客户提供的原始 `.xlsx` 模板文件，确认：
     - 模板是单 Sheet 还是多 Sheet；
     - 表头固定在第几行；
     - 是否需要保留公式、图片、签名栏；
     - 送货单和对账单分别哪个文件。

5. **付款申请单/报销单**
   - 按当前分析继续使用 Django 实现，暂缓开发；待模板确认后复用模板生成机制。
