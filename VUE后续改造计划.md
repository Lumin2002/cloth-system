# Vue 后续改造计划

## 一、当前状态

项目已经完成第一阶段 Vue 化，主要完成了：

- 新增/编辑/详情编辑订单的交互
- 客户管理列表
- 布种编号列表
- 面料报价列表
- 供应商管理列表
- 库存日志列表
- 仪表盘汇总区
- 供应商端仪表盘汇总卡片
- 库存列表统计卡片和分页
- 订单列表统计卡片和分页
- 统一分页组件

## 二、还未完成 Vue 化的位置

### 高优先级

1. 订单列表表格、筛选、批量操作
2. 库存列表表格、筛选、批量操作
3. 供应商端订单表格和筛选
4. 供应商订单详情页表单和出货交互

### 中优先级

5. 订单详情只读页
6. 库存表单
7. 布种表单
8. 面料报价表单
9. 供应商管理表单

### 低优先级

10. 通知页面
11. 系统监控
12. 数据库备份
13. 系统设置
14. 仪表盘图表目前仍是 ECharts，未 Vue 化

## 三、建议实施顺序

### 第一步：供应商端订单表格和筛选

- 目标：供应商登录后看到的订单列表
- Vue 负责：
  - 搜索
  - 状态筛选
  - 日期筛选
  - 订单表格渲染
- 保留：
  - 供应商报价、出货跳转到原详情页

### 第二步：订单详情只读页

- 目标：订单详情展示信息
- Vue 负责：
  - 基本信息、布料信息、财务信息展示
  - 进度阶段展示
- 保留：
  - 编辑、删除、对账单等操作链接

### 第三步：库存列表表格、筛选、批量操作

- 目标：库存主列表
- Vue 负责：
  - 搜索
  - 客户、位置、数量筛选
  - 表格渲染
  - 分页
- 保留：
  - 导入、导出、清空、批量操作仍走后端接口

### 第四步：订单列表表格、筛选、批量操作

- 目标：订单主列表
- Vue 负责：
  - 搜索、筛选、排序
  - 订单表格渲染
  - 分页
- 保留：
  - 新增、导入、导出、删除全部
  - 批量修改付款状态、批量导出等操作

### 第五步：表单页

- 目标：库存、布种、报价、供应商表单
- 方式：
  - 先复用现有 Vue 表单交互
  - 逐步将字段校验、自动补全、动态选项迁移到 Vue

### 第六步：辅助页面

- 通知、监控、备份、设置
- 按需评估，不建议一次性全部改造

## 四、技术原则

1. 继续保持 Django 服务端渲染为主，Vue 只负责局部交互。
2. 涉及批量删除、导出、付款状态修改的操作，优先保留 Django 后端接口，避免前端重复处理权限。
3. 统一使用现有 Vue 分页组件。
4. Vue 插值符继续使用 `[[ ]]`，避免和 Django `{{ }}` 冲突。
5. 数据量大的列表优先保留服务端分页，不把所有数据一次性渲染。

## 五、风险点

### 订单列表

- 字段多，金额计算复杂
- 批量操作涉及删除、导出、状态修改
- 必须保留当前筛选条件

### 库存列表

- 数据量大
- 批量操作涉及删除和导出
- 库存数量筛选规则较多

### 供应商端

- 权限隔离必须保持
- 报价、出货操作不能因为 Vue 化而丢失权限校验

## 六、完成标准

当以下条件都满足时，可以认为 Vue 改造基本完成：

- 主要业务页面交互由 Vue 管理
- 批量操作和权限仍然稳定
- 分页、搜索、筛选体验一致
- Django 仅负责页面壳、初始数据和后端接口
